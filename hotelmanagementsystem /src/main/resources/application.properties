# Database connection details
spring.datasource.url=jdbc:mysql://localhost:3306/hotels
spring.datasource.username=root
spring.datasource.password=sarika@2006
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate settings

# Consider switching to 'validate' or 'none' in production
spring.jpa.hibernate.ddl-auto=update  

 # Show SQL queries in the console for debugging
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Log SQL queries
logging.level.org.hibernate.SQL=DEBUG  

 # Log schema generation details (optional, can be set to INFO or WARN in production) # Enable detailed logging for Hibernate queries
logging.level.org.hibernate.tool.hbm2ddl=TRACE 



